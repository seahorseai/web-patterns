export class OrderProcessor {
    placeOrder(productId: number, quantity: number): void {
        console.log(`Placing order for product ${productId}`);
        // Simulate order placement logic
        console.log(`Order placed successfully for product ${productId}`);
    }
}